name: Deploy Firestore and Realtime Database Rules

on:
  push:
    branches:
      - master
    paths:
      - 'firebase/firestore.rules'
      - 'firebase/firestore.indexes.json'
      - 'firebase/rtdb.rules.json'

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v7

      - name: Deploy Firestore and Realtime Database Rules
        uses: w9jds/firebase-action@v15.21.0
        with:
          args: deploy --only firestore:rules,firestore:indexes,database:rules
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
